gates:
  - name: deny-marker
    kind: regex
    scan:
      kind: body
      action:
        kind: deny
    pattern_catalog:
      entities:
        - name: unsafe-marker
          rules:
            - name: exact-deny-marker
              pattern: DENY_THIS
              confidence: high
  - name: replace-marker
    kind: regex
    scan:
      kind: body
      action:
        kind: replace
        template: "[REDACTED]"
    pattern_catalog:
      entities:
        - name: replacement-marker
          rules:
            - name: exact-replacement-marker
              pattern: REDACT_THIS
              confidence: high
default_decision: allow
