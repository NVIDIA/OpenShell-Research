version: 1

filesystem_policy:
  include_workdir: true
  read_only: [/usr, /lib, /proc, /dev/urandom, /app, /etc, /var/log]
  read_write: [/sandbox, /tmp, /dev/null]
landlock:
  compatibility: best_effort
process:
  run_as_user: sandbox
  run_as_group: sandbox

network_policies:
  claude_code:
    name: Claude Code subscription access
    endpoints:
      - host: api.anthropic.com
        port: 443
        protocol: rest
        enforcement: enforce
        access: full
      - host: platform.claude.com
        port: 443
        protocol: rest
        enforcement: enforce
        access: full
      - host: claude.ai
        port: 443
      - { host: statsig.anthropic.com, port: 443 }
      - { host: sentry.io, port: 443 }
    binaries:
      - { path: /usr/local/bin/claude }
      - { path: /usr/bin/node }

network_middlewares:
  egress_gate_replace:
    name: Replace email addresses and customer IDs
    middleware: egress-gate-redaction
    order: 0
    config:
      pipeline:
        gates:
          - name: identifiers
            config:
              gate: regex-body
              pattern_catalog: patterns.yaml
              mode: replace
              replacement:
                strategy: template
                template: "[{entity}]"
        default_decision: allow
    on_error: fail_closed
    endpoints:
      include:
        - api.anthropic.com
