pipeline:
  gates:
    - name: identifiers
      config:
        gate: regex-body
        pattern_catalog: patterns.yaml
        mode: replace
        replacement:
          strategy: template
          template: "[{entity}]"
  default_decision: allow
