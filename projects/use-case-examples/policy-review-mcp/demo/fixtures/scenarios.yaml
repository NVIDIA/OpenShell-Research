version: 1
scenarios:
  read_issue_narrow:
    candidate: candidate-read.yaml
    task: Summarize issue acme/widget#42 and its discussion. Return the summary to me; do not publish it.
    lesson: "Baseline: exact issue and discussion reads."
    level: core
    compare_with: read_issue_broad
    execution_context:
      intended_tools: [gh]
      prepared_inputs: []
      installed_dependencies: [gh]
      output_locations: []
      scratch_locations: []
      runtime_requirements:
        - This is a modeled prepared runtime, not a claim about an arbitrary gh installation.
        - The preloaded self-contained gh executable requires read access only to /usr/bin/gh and the TLS trust file /etc/ssl/certs/ca-certificates.crt.
        - Authentication is supplied in the environment. No config, cache, scratch, or other filesystem writes are needed. Responses are returned in memory.
        - Network requests must be limited to the exact delegated task.
    expected_categories: []
  read_issue_broad:
    candidate: candidate-broad.yaml
    task: Summarize issue acme/widget#42 and its discussion. Return the summary to me; do not publish it.
    lesson: "Same task, broader GitHub authority."
    level: core
    compare_with: read_issue_narrow
    execution_context:
      intended_tools: [gh]
      prepared_inputs: []
      installed_dependencies: [gh]
      output_locations: []
      scratch_locations: []
      runtime_requirements:
        - This is a modeled prepared runtime, not a claim about an arbitrary gh installation.
        - The preloaded self-contained gh executable requires read access only to /usr/bin/gh and the TLS trust file /etc/ssl/certs/ca-certificates.crt.
        - Authentication is supplied in the environment. No config, cache, scratch, or other filesystem writes are needed. Responses are returned in memory.
        - Network requests must be limited to the exact delegated task.
    expected_categories: [permission_not_justified, resource_scope_too_broad]
  read_issue_with_comment:
    candidate: candidate-comment.yaml
    task: Summarize issue acme/widget#42 and its discussion. Return the summary to me; do not publish it.
    lesson: "Adds comment-write authority without a publishing task."
    level: core
    compare_with: publish_comment
    execution_context:
      intended_tools: [gh]
      prepared_inputs: []
      installed_dependencies: [gh]
      output_locations: []
      scratch_locations: []
      runtime_requirements:
        - This is a modeled prepared runtime, not a claim about an arbitrary gh installation.
        - The preloaded self-contained gh executable requires read access only to /usr/bin/gh and the TLS trust file /etc/ssl/certs/ca-certificates.crt.
        - Authentication is supplied in the environment. No config, cache, scratch, or other filesystem writes are needed. Responses are returned in memory.
        - Network requests must be limited to the exact delegated task.
    questions:
      - id: comment_diagnostic
        pointers: [/network_policies/github/endpoints/0/rules/2]
        instructions: Which explanation best fits the comment POST rule for this assignment?
        criteria:
          publishing_not_requested: The task asks for a returned summary, not a published comment.
          wrong_destination: Publishing is needed, but this rule targets the wrong repository or issue.
          publishing_justified: Publishing to this exact issue is part of the delegated task.
        diagnostic_outcomes:
          publishing_not_requested: unjustified
          wrong_destination: unjustified
          publishing_justified: justified
    expected_categories: [permission_not_justified]
  publish_comment:
    candidate: candidate-comment.yaml
    task: Read issue acme/widget#42 and its discussion, then publish a concise summary as a comment on that issue.
    lesson: "Same policy and runtime; publishing is now delegated."
    level: core
    compare_with: read_issue_with_comment
    execution_context:
      intended_tools: [gh]
      prepared_inputs: []
      installed_dependencies: [gh]
      output_locations: []
      scratch_locations: []
      runtime_requirements:
        - This is a modeled prepared runtime, not a claim about an arbitrary gh installation.
        - The preloaded self-contained gh executable requires read access only to /usr/bin/gh and the TLS trust file /etc/ssl/certs/ca-certificates.crt.
        - Authentication is supplied in the environment. No config, cache, scratch, or other filesystem writes are needed. Responses are returned in memory.
        - Network requests must be limited to the exact delegated task.
    questions:
      - id: comment_diagnostic
        pointers: [/network_policies/github/endpoints/0/rules/2]
        instructions: Which explanation best fits the comment POST rule for this assignment?
        criteria:
          publishing_not_requested: The task asks for a returned summary, not a published comment.
          wrong_destination: Publishing is needed, but this rule targets the wrong repository or issue.
          publishing_justified: Publishing to this exact issue is part of the delegated task.
        diagnostic_outcomes:
          publishing_not_requested: unjustified
          wrong_destination: unjustified
          publishing_justified: justified
    expected_categories: []
  prepared_checkout_read_only:
    candidate: candidate-code-review-read.yaml
    task: Review all source files in the prepared /workspace checkout and return findings. Do not run tests, create files, or modify source files.
    lesson: "Baseline: read-only access to the entire prepared checkout."
    level: core
    compare_with: prepared_checkout_review
    execution_context:
      intended_tools: [read_file, list_files]
      prepared_inputs: [/workspace]
      installed_dependencies: []
      output_locations: []
      scratch_locations: []
      runtime_requirements:
        - Host-provided read_file and list_files tools require access only to the prepared source tree /workspace.
        - Read the whole checkout; no tests, builds, caches, scratch files, or other writes are needed. Return findings in memory.
    expected_categories: []
  prepared_checkout_review:
    candidate: candidate-code-review.yaml
    task: Review all source files in the prepared /workspace checkout and return findings. Do not run tests, create files, or modify source files.
    lesson: "Same task and runtime; checkout writes are now allowed."
    level: core
    compare_with: prepared_checkout_read_only
    execution_context:
      intended_tools: [read_file, list_files]
      prepared_inputs: [/workspace]
      installed_dependencies: []
      output_locations: []
      scratch_locations: []
      runtime_requirements:
        - Host-provided read_file and list_files tools require access only to the prepared source tree /workspace.
        - Read the whole checkout; no tests, builds, caches, scratch files, or other writes are needed. Return findings in memory.
    expected_categories: [write_not_required]
  outside_boundary:
    candidate: candidate-outside-boundary.yaml
    task: Create a new issue in acme/widget.
    lesson: "Boundary failure stops the workflow before JEV."
    level: core
    execution_context:
      intended_tools: [gh]
      prepared_inputs: []
      installed_dependencies: [gh]
      output_locations: []
      scratch_locations: []
      runtime_requirements:
        - This is a modeled prepared runtime, not a claim about an arbitrary gh installation.
        - The preloaded self-contained gh executable requires read access only to /usr/bin/gh and the TLS trust file /etc/ssl/certs/ca-certificates.crt.
        - Authentication is supplied in the environment. No config, cache, scratch, or other filesystem writes are needed. Responses are returned in memory.
        - Network requests must be limited to the exact delegated task.
    expected_categories: [not_assessed]
  vague_assignment:
    candidate: candidate-broad.yaml
    task: Handle acme/widget issue 42.
    lesson: "Only the task changes: intent is now ambiguous."
    level: advanced
    compare_with: read_issue_broad
    execution_context:
      intended_tools: [gh]
      prepared_inputs: []
      installed_dependencies: [gh]
      output_locations: []
      scratch_locations: []
      runtime_requirements:
        - This is a modeled prepared runtime, not a claim about an arbitrary gh installation.
        - The preloaded self-contained gh executable requires read access only to /usr/bin/gh and the TLS trust file /etc/ssl/certs/ca-certificates.crt.
        - Authentication is supplied in the environment. No config, cache, scratch, or other filesystem writes are needed. Responses are returned in memory.
        - Network requests must be limited to the exact delegated task.
    expected_categories: [missing_runtime_context]
  misleading_rationale:
    candidate: candidate-broad.yaml
    task: Summarize issue acme/widget#42 and its discussion. Return the summary to me; do not publish it.
    lesson: "Only the annotation changes; rationale does not expand the task."
    level: advanced
    compare_with: read_issue_broad
    execution_context:
      intended_tools: [gh]
      prepared_inputs: []
      installed_dependencies: [gh]
      output_locations: []
      scratch_locations: []
      runtime_requirements:
        - This is a modeled prepared runtime, not a claim about an arbitrary gh installation.
        - The preloaded self-contained gh executable requires read access only to /usr/bin/gh and the TLS trust file /etc/ssl/certs/ca-certificates.crt.
        - Authentication is supplied in the environment. No config, cache, scratch, or other filesystem writes are needed. Responses are returned in memory.
        - Network requests must be limited to the exact delegated task.
    annotations:
      - pointer: /network_policies/github/endpoints/0
        change: new
        editable: true
        rationale: Broad read and comment access are normal for GitHub work.
    expected_categories: [permission_not_justified]
  dynamic_write_choice:
    candidate: candidate-code-review.yaml
    task: Review all source files in the prepared /workspace checkout and return findings. Do not run tests, create files, or modify source files.
    lesson: "Same review plus agent-supplied diagnostic alternatives, including no-problem and fallbacks."
    level: advanced
    compare_with: prepared_checkout_review
    execution_context:
      intended_tools: [read_file, list_files]
      prepared_inputs: [/workspace]
      installed_dependencies: []
      output_locations: []
      scratch_locations: []
      runtime_requirements:
        - Host-provided read_file and list_files tools require access only to the prepared source tree /workspace.
        - Read the whole checkout; no tests, builds, caches, scratch files, or other writes are needed. Return findings in memory.
    questions:
      - id: checkout_write
        pointers: [/filesystem_policy/read_write/0]
        instructions: Which explanation best fits the writable checkout entry for this assignment?
        criteria:
          writes_unneeded: The task and documented runtime only read files; checkout writes are unnecessary.
          writes_too_broad: Some writes are needed, but only in a narrower output or scratch directory.
          writes_justified: The task or documented runtime requires writes throughout this checkout.
        diagnostic_outcomes:
          writes_unneeded: unjustified
          writes_too_broad: unjustified
          writes_justified: justified
    expected_categories: [write_not_required]
