# Paging helper

`take_page(items, limit)` returns up to `limit` items from the start of a
sequence. A positive limit is required. The project supports Python 3.12.

Run `python3 -m unittest discover -s tests`.
