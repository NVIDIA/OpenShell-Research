# Order summary prototype

This internal prototype calculates an order total from integer cent amounts.
It is used by one team and deliberately has no plugin system, persistence layer,
or compatibility policy. Run `python3 -m unittest discover -s tests`.
