Read the input file at `{{ oar.input_path }}` with the read tool.
Call `submit_result` with its text content (excluding leading/trailing whitespace)
in `content` and the exact marker `{{ marker }}` in `marker`. Do not review,
summarize, or rewrite the input. If submission fails validation, correct it.
